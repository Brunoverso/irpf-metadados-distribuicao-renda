# Outputs Directory

This directory stores generated research assets.

Assets should be grouped by module, then by type:

- `metadata/tables/`: generated table assets for the source-metadata module.
- `metadata/figures/`: generated figures for the source-metadata module.

File names should identify the module, content, and format clearly enough for other researchers to understand their role without opening them.
